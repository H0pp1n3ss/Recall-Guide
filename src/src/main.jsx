import React from "react";
import ReactDOM from "react-dom/client";
import RecallTraining from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <RecallTraining />
  </React.StrictMode>
);
